## Requirement
- vscode
- nodemon
    - `npm i -g nodemon`

## Run
- vscode -> `Launch` / `Launch via nodemon`

## Test
- vscode -> `Debug Jest Tests (Default/Windows)`

## Docker
`docker-compose up`
`docker-compose build`
